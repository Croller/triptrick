import styled from 'styled-components';
import { THEME } from 'client/style/constants';

export const Wrapper = styled.label`
  font-size: ${THEME.controlFontSize};
  margin-bottom: ${THEME.controlLabelMargin};
`;
